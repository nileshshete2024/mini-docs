Frontend (React) communicates with backend (Node.js + Express) via REST APIs.

Data is stored in SQLite.

Decisions:
- Used TipTap for quick rich text support
- Used SQLite for simplicity
- Simplified sharing model

Tradeoffs:
- No authentication
- No real-time collaboration